{
  "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }],
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm run build",
  "devCommand": "pnpm run build",
  "installCommand": "pnpm i",
  "framework": null,
  "outputDirectory": "./storybook-static"
}
