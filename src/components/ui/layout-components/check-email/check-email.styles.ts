import clsx from 'clsx'

import s from './check-email.module.scss'

export const cn = {
  button: s.button,
  confirm: clsx(s.button, s.confirm),
  container: s.container,
  form: s.form,
  icon: s.icon,
  input: s.input,
  reminder: s.reminder,
  title: s.title,
}
