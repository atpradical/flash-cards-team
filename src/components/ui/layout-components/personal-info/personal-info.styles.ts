import s from './personal-info.module.scss'

export const cn = {
  avatar: s.avatar,
  bottom: s.bottom,
  container: s.container,
  editAvatar: s.editAvatar,
  hint: s.hint,
  icon: s.icon,
  wrapper: s.wrapper,
}
