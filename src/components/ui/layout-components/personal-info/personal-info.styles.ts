import s from './personal-info.module.scss'

export const cn = {
  avatar: s.avatar,
  bottom: s.bottom,
  container: s.container,
  delete: s.delete,
  edit: s.edit,
  hint: s.hint,
  icon: s.icon,
  title: s.title,
  wrapper: s.wrapper,
}
