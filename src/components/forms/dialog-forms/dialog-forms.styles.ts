import s from './dialog-forms.module.scss'

export const cn = {
  close: s.close,
  container: s.container,
  delete: s.delete,
  form: s.form,
  icon: s.icon,
  relative: s.relative,
  trash: s.trash,
}
