import clsx from 'clsx'

import s from './dialog-forms.module.scss'

export const cn = {
  close: clsx(s.close),
  container: clsx(s.container),
  delete: clsx(s.delete),
  form: clsx(s.form),
  icon: clsx(s.icon),
  relative: clsx(s.relative),
  trash: clsx(s.trash),
}
